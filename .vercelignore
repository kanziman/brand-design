# Keep the public deploy to the landing page (index.html) + the app (momento/).
# Everything below stays in the repo but is not uploaded to Vercel.

.DS_Store
.vscode/
.mcp.json

# Internal product docs (PRD, ui-spec, etc.)
docs/
*.md

# SNS release carousel (image-generation source, not a hosted page)
carousel/

# README preview screenshots (used only by README.md on GitHub)
previews/

# Earlier design explorations / mockups (not part of the shipped site)
archive/
